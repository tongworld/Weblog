<h1>The Weblog</h1>
<%= link_to 'New post', new_post_path %>
<br />
<% for post in @posts.reverse %>
  <h2><%= post.title %></h2>
  <p><%= post.body %></p>
  <small>
<!--
      <%= link_to "permalink",
      :action => "show",
      :id => post %>
-->
    <%= link_to 'permalink', edit_post_path(post) %>
  </small>
  <hr />
<% end %>
<br />
<br />
<br />

<%= link_to 'New post', new_post_path %>

<!--
<h1>Listing posts</h1>

<table>
  <tr>
    <th>Title</th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%=h post.title %></td>
    <td><%= link_to 'Show', post %></td>
    <td><%= link_to 'Edit', edit_post_path(post) %></td>
    <td><%= link_to 'Destroy', post, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New post', new_post_path %>
-->

